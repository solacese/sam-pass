# sam-pass

A static operations dashboard showing high-priority device incidents on an interactive map.

## Local preview

```sh
python3 -m http.server 8000
```

Then open <http://localhost:8000>.

## GitHub Pages

The site is served from the repository root on the `main` branch.
